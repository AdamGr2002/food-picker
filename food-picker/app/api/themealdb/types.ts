export type FoodItem = {
    id: string
    title: string
    image: string
    category: string
    instructions: string
    ingredients: string[]
    area: string
  }
  
  export type Category = string